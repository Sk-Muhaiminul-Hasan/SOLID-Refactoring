# SOLID Design Principles
This repository contains refactored solutions of the SOLID design principles which were violated in the original code.

## Student Information
Name: Shaikh Muhaiminul Hasan

ID: 230042113

Course: SWE 4301 (OOC-II)

## Assignment: Assignment 2 / Quiz 4 Solution

Original code was provided in link[https://github.com/jahs-es/solid-kata?authuser=1].

Core Principles
1. Single Responsibility Principle (SRP)
“A class should have one, and only one, reason to change.”
Every class should map to a single part of the functionality provided by the software, and that responsibility should be entirely encapsulated by the class.

2. Open/Closed Principle (OCP)
“Software entities should be open for extension, but closed for modification.”
You should be able to add new functionality to a class (extension) without altering its existing source code (modification).

3. Liskov Substitution Principle (LSP)
“Derived classes must be substitutable for their base classes.”
Objects of a superclass should be replaceable with objects of its subclasses without breaking the application. Essentially, a subclass should never weaken the preconditions or strengthen the postconditions of the parent.

4. Interface Segregation Principle (ISP)
“Clients should not be forced to depend on interfaces they do not use.”
Instead of one "fat" interface, many small, client-specific interfaces are preferred so that implementing classes only need to concern themselves with methods that are relevant to them.

5. Dependency Inversion Principle (DIP)
“Depend on abstractions, not on concretions.”
High-level modules should not depend on low-level modules; both should depend on abstractions (interfaces). Furthermore, abstractions should not depend on details; details should depend on abstractions.

## Solution by:
### Shaikh Muhaiminul Hasan
### Id 230042113
### Course: SWE 4301 (OOC-II)